# 政策文件解析知识图谱

更新时间：2026-09-16 11:56:58

## 1. 总目标

把航司政策文件解析成可进入配置系统的结构化字段，并通过审核页面形成闭环：

1. 读取原始 DOC/DOCX/PDF/XLS/XLSX。
2. 先判定文件类型，再套用对应字段集。
3. 每个字段只从当前文件原文、当前附件或明确同源表格提取。
4. 不确定但属于当前文件类型核心字段时，输出 `人工确认`。
5. 审核员修改后，保存字段值、修改历史、问题标记。
6. 把人工修改沉淀为规则，只对同类型、同语义的未确认文件重新解析，不复制其他文件的值。
7. 已确认结果进入最终结果页或接口调用。

## 2. 顶层决策图

```
原始文件
  ↓
提取正文/表格/PDF 文本
  ↓
是否缺少核心附件？
  ├─ 是：停止该文件解析，提示补附件
  └─ 否
       ↓
场景分类：次卡 / 旗舰店 code / 旗舰店券类 / 自营
       ↓
套用该场景字段集
       ↓
字段逐项提取 + 证据绑定
       ↓
质量门禁
       ├─ 字段不在当前场景：删除/不展示
       ├─ 字段有证据：输出具体值
       ├─ 核心字段无法确定：人工确认
       └─ 可选字段未出现：空/不展示
       ↓
审核页面
       ↓
人工修改/新增/删除/标记问题
       ↓
本地审核存储 + 版本记录 + 规则沉淀
```

## 3. 场景分类规则

### 3.1 先判定文件类型

一个文件只能有一个当前文件类型：

- `次卡`
- `旗舰店code`
- `旗舰店券类`
- `自营`

分类不能使用用户口头提示直接定死。用户指出错误时，只作为复核入口，必须回到文件正文找证据。

### 3.2 次卡

命中信号：

- 文件名或正文出现：次卡、权益卡、共享卡、畅游卡、无限飞、往返卡。
- 核心业务是购卡、兑换、使用次数、兑换航班、兑换日期、产品价格档位。

重点字段：

- `sale_date`：销售/购卡日期。
- `use_date` / `redeem_date`：兑换/使用日期。
- `depart_date`：航班/旅行/出行日期。
- `usage_times`：可使用次数。
- `travel_class`：舱等。
- `price_groups_summary`：价格组汇总。
- `product_show`：权益卡展示文案。

关键约束：

- 次卡 JSON 只按 `flight_type`、`travel_class`、`usage_times` 拆分。
- 价格档位进入 `price_groups` 或价格组汇总，不按价格档位盲目拆 JSON。
- 往返/往返拼接/单次往返次卡，航线价格必须是可售卡包或往返组合价，不能把单段价当最终价。
- 出现 `见附件/详见航线表/以附件为准` 且附件缺失时，停止解析并提醒补附件。

### 3.3 旗舰店 code

命中信号：

- 明确出现：旗舰店、官网旗舰店、直连合作方、授权销售渠道、授权销售代理、旗舰店客票、直连客票、BSP 客票分支、TC 项、产品代码、产品操作规定。
- 长龙类业务通告虽然有航司盖章/通告格式，但如果正文是产品操作规定、授权销售渠道、TC 项、产品代码、权益产品售卖配置，应判为旗舰店 code。

关键约束：

- 旗舰店不看 PAT:A/出票指令。
- 不得把 PAT:A 写入 `product_type`。
- 不得因为缺少 PAT:A 而把字段标为人工确认。
- HU/海航 `product_code` 需要拼接 `TOURISMROVER_` 前缀。

### 3.4 旗舰店券类

命中信号：

- 优惠券、券码、资源券、消费券、coupon、资源编号+优惠券、优惠码。

关键约束：

- 优惠码和资源编号按原文标签区分，不按编码形状猜。
- 销售日期等于券使用/购票日期时，直接用原文日期，不把结束日期 +1 天。
- 多券码文件要完整输出券码，不得只取样例。

### 3.5 自营

命中信号：

- 客运销售运价政策、内部运价政策、运价通告、私有运价投放单。
- 正文核心是航司内部运价投放、票台出票、PAT:A 指令。
- “投放去哪儿/携程”等仅作为分销渠道之一，不足以判为旗舰店。

关键约束：

- `product_type` 可输出 PAT:A/票价指令。
- 深航/国航等 `PAT:A#CGP` / `PAT:A#GP` 中 GP/CGP 若只是政府票/大客户标识，`product_type` 输出 `PAT:A`，GP/CGP 放入证据或 `corporate_code`。
- 成人/儿童 PAT 指令必须完整保留，例如 `成人:PAT:A；儿童:PAT:A*CH`。

## 4. 字段地图

### 4.1 基础信息

- `scenario`：解析场景。
- `business_line`：业务线，先由场景决定，再看渠道。
- `carrier` / `carrier_name`：航司二字码和航司名称。
- `product_name` / `product_family`：产品名称和产品类型。
- `policy_code`：政策编号或文号。

### 4.2 产品代码与出票

- `product_code`：产品/活动代码。HU 旗舰店产品必须拼接 `TOURISMROVER_`。
- `fare_basis`：运价基础。
- `corporate_code`：大客户编码，从大客户编码、特殊指令、TC 项、`#C...` 中提取。
- `product_type`：自营为出票/价格指令；旗舰店为产品类型，不写 PAT:A。

### 4.3 适用范围

- `flight_type`：单程/往返/往返拼接等。
- `od`：适用航线，统一输出 OD 对。
- `not_applicable_od` / `not_applicable_flight_routes`：不适用航线/航班/日期/舱位。
- `booking_cabin` / `cabin`：适用舱位和舱位代码。

### 4.4 日期规则

- `sale_date`：销售日期、售卖日期、购卡日期。
- `use_date` / `redeem_date`：兑换/使用日期。
- `depart_date`：旅行/航班/出行日期。
- `advance_purchase_days`：提前购票天数。

日期规则：

- `即日起/上线之日起/产品生效之日起`：未来/实时政策用解析当天；历史政策优先用发文/发布/签发/生效日期；无法确定才人工确认。
- 只有结束日期时，输出可恢复的一端，例如 `人工确认至2026-05-31`，不能丢掉结束日。
- 文件只写 `适用日期` 时，只有当业务语义确认销售和乘机同周期，才能同时填 `sale_date` 和 `depart_date`。

### 4.5 优惠与旅客

- `discount_type`：直减金额、直减比例、指定票面价、直加金额等。
- `discount_per`：金额/比例，必须带单位或百分号。
- `age_limit`：年龄限制。
- `document_restrictions`：证件限制。年龄限制产品默认 `居民身份证`，除非原文明确允许其他证件。
- `child_applicable` / `baby_applicable`：儿童/婴儿适用性必须有原文证据。
- `membership_limit`：只有购票资格要求会员身份时才填；注册会员领取权益不等于会员限制。

### 4.6 退改签

- `change_rules`：变更、改期、升舱、航班/日期/舱位调整。
- `sign_and_transfer_rules`：签转、EI、外航、不得签转。
- `refund_rules`：退票、退款、仅退税费、退票导致权益取消。

拆分规则：

- `退改签` 是章节标题，不是字段值。
- 必须读取整个章节，直到下一个业务大标题。
- 权益后果跟随触发动作：退票取消权益放退票规则；自愿改期取消权益放变更规则；签转外航取消权益放签转规则。

## 5. 权益名称与权益使用规则

这是本次重点修复的两个字段。

### 5.1 正确定义

`benefit_name` / 权益名称：

- 表示旅客获得的具体附加权益。
- 示例：酒店券、快速安检服务、优先选座、托运行李额、升舱权益、积分/里程、休息室、餐食、接送机。
- 不是产品名称、营销活动名称、政策名称、人工确认项，也不是退改签章节标题。

`benefit_usage_rules` / 权益使用规则：

- 只表示该权益如何领取、激活、兑换、使用、有效期、适用范围、退款/改签后的权益处理。
- 不得把整篇产品规则、航线范围、票价规则、退改签全文都塞入该字段。

### 5.2 输出前置条件

同时满足才输出：

1. 当前文件类型的标准字段集包含该字段，或审核员已把该字段作为当前文件类型的人工新增字段定义沉淀下来。
2. 当前文件自身原文有明确权益证据。
3. 字段值能从当前文件提取，不能从别的文件或历史修改复制。
4. 一份文档只输出一次：`权益名称`、`权益使用规则` 是文档级字段，不跟随航线、价格、日期、券码或其它配置模块重复输出。

不满足时：

- 不输出。
- 不显示为 `人工确认`。
- 不在页面占位。

### 5.3 本次线上问题根因

旧实现中，页面字段准入有一个兜底：

```
字段属于已知中文标签 && 旧数据里有具体值 => 允许展示
```

这导致两类相反问题：

- 仅有历史残留值、没有当前类型字段定义的文件，也会展示 `benefit_name/benefit_usage_rules`。
- 上一轮修复把源文绑定字段拦截放在人工字段定义之前，导致审核员明确新增过、且当前文件有真实权益证据的字段也被挡掉。

同时，旧场景识别把 `权益/礼包/升舱/安检/行李` 这类普通业务词也当成旗舰店信号，容易把自营的 `机+酒店`、礼包权益后果误判成旗舰店。

### 5.4 已修复规则

- `benefit_name/benefit_usage_rules` 不再走“有值即展示”的兜底。
- 先判断标准字段集，再判断当前文件类型的人工字段定义；两者都没有时，才过滤历史残留权益字段。
- 自营/次卡文件如果已经有人工字段定义，并且当前文件原文有具体权益证据，仍然可以展示并输出这两个字段。
- `benefit_name/benefit_usage_rules` 统一归属为文档级权益信息，一份文件最多一组；多模块/多配置单元不重复展示、不重复写入。
- 场景识别不再仅凭 `权益/礼包/升舱/安检/行李` 判旗舰店。
- 已恢复深航“机+酒店”静态记录中的自营权益字段定义和值，因为该文件原文确有“赠深航酒店券一张”和完整使用规则。
- 龙航“积分优享”“快速安检”属于旗舰店 code，仍保留权益字段。

## 6. 人工确认与学习闭环

### 6.1 人工确认

`人工确认` 只用于当前文件类型的核心字段，而且确实无法从当前文件稳定判断。

不得把以下内容写入人工确认：

- 场景分类理由。
- 解析思路。
- “旗舰店不解析 PAT:A”这类规则说明。
- 可选字段未出现。
- 其他文件中曾经出现过的字段。

### 6.2 人工修改

当审核员把 `人工确认/需人工确认/未提取` 改成具体值：

1. 页面立即展示新值。
2. 清理 `manual_confirm_fields` 中对应字段。
3. 写入修改历史。
4. 自动标记为有问题。
5. 把修改原因转化为解析规则。
6. 对同场景、同语义、未确认文件重新读取原文后矫正。

### 6.3 人工新增字段

新增字段必须包含：

- 字段中文名。
- JSON key。
- 字段格式：string、boolean、number、array、object 等。
- 字段简述。
- 字段内容。
- 提取逻辑。
- 文件类型：次卡、旗舰店 code、旗舰店券类、自营。

新增字段和原字段没有展示差异，都是同一套字段行样式。

重点约束：

- 新增的是字段定义和提取逻辑，不是把当前文件的字段值复制给其他文件。
- 后续同类型文件要按自己的原文重新提取。
- 如果原文没有对应证据，可为空；不应强行展示 `人工确认`。

### 6.4 人工删除字段

删除字段表示该字段对当前文件或当前文件类型不适用。

- 如果是场景字段集错误，后续同场景同范围不再展示该字段。
- 如果只是当前文件无证据，只清理当前文件。
- 删除结果必须同步到页面当前值、最终 JSON、修改历史和本地审核存储。

## 7. 质量门禁

解析或上传前必须检查：

1. 文件类型是否唯一。
2. 输出字段是否属于当前文件类型字段集。
3. `人工确认` 是否只出现在当前类型核心字段。
4. `benefit_name/benefit_usage_rules` 是否有当前文件权益证据，且属于标准字段集或当前文件类型的人工字段定义。
5. 退改签是否完整拆分，不把标题当值。
6. 日期是否保留原文边界，不随意 +1 天。
7. 次卡往返价格是否使用组合价，不用单段价冒充。
8. `见附件` 核心信息缺附件时是否停止解析。
9. HU `product_code` 是否拼接 `TOURISMROVER_`。
10. 年龄限制产品证件限制是否补 `居民身份证`。
11. 优惠码、资源编号、大客户编码、政策编号是否未混淆。
12. 人工修改后的字段是否真正覆盖旧值并清除对应人工确认项。

## 8. 当前线上修复记录

时间：2026-09-16 11:56:58

触发问题：

- 审核页曾出现两类问题：不该有权益字段的文件展示了权益字段；上一轮修复后，该有权益字段的深航“机+酒店”等文件又被过滤。
- 审核员手工添加这两个字段的真实意图是：把“权益名称”和“权益使用规则”作为普通解析字段沉淀下来，但字段值必须来自当前文件自己的权益段落。

根因：

- 前端字段准入允许“已知字段 + 有具体值”绕过当前文件类型字段集，造成旧值污染。
- 场景识别把 `权益/礼包/升舱/安检/行李` 作为过强旗舰店信号。
- 上一轮修复把源文绑定字段拦截放在人工字段定义之前，误拦了已人工新增、且对当前文件有效的字段。

修复：

- 收紧 `benefit_name/benefit_usage_rules` 展示规则：必须属于标准字段集，或属于当前文件类型的人工新增字段定义。
- 修正页面分组逻辑：人工新增字段不能被追加到每一个分组；已有分组字段只在所属分组展示一次，未归属字段统一进入“其他信息”分组。
- 修正数组型解析结果写入逻辑：权益字段作为文档级字段，只写一次，不写入每个拆分配置单元。
- 服务端本地状态输出时只过滤“没有字段定义支撑”的历史残留权益字段。
- 恢复深航“机+酒店”记录中的自营权益字段定义和值。
- 更新本地 `parse-airline-policy` Skill 和历史 case 规则。

影响范围：

- 不该有权益字段的文件不再显示历史残留值。
- 该有权益字段且有当前文件证据的文件继续显示权益字段，包括通过人工字段定义扩展到自营/次卡的文件。
- 当前静态数据中保留权益字段的文件为：
  - `XLS-CP-38[2025] 关于重新下发《长龙航空“积分优享”产品》的业务通告（2025年第二次修定）--最新版本.pdf`
  - `XS-CP-157[2024]关于重新下发《长龙航空“快速安检”产品》的业务通告（最新版）.pdf`
  - `2025年深航“机+酒店”产品手册(新）.docx`

## 9. 给后续解析模型的执行口诀

先分类，后字段；先证据，后结论；先当前文件，后历史经验。

权益字段不是垃圾桶，人工确认不是占位符，历史修改不是值模板。

任何字段都必须回答三个问题：

1. 当前文件类型该不该有这个字段？
2. 当前文件原文有没有足够证据？
3. 输出值是否能直接用于配置或审核？
